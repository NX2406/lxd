# 查看访问路径

如果忘记了面板的访问路径，在服务器上执行：

```bash
cat /opt/lxd-panel/ACCESS_PATH.txt
```

这将显示你的随机访问路径，例如：`/panel-a1b2c3d4`

完整访问地址: `http://你的服务器IP/panel-a1b2c3d4`

## 安全说明

- 直接访问 `http://服务器IP/` 会返回404
- 只有通过正确的随机路径才能访问面板
- 请妥善保管这个路径，不要泄露给他人
- 建议配置HTTPS以进一步提高安全性
